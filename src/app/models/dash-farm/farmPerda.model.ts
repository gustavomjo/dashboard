export interface farmPerda{
  data : Date,
  ds_setor : string,
  validade : Date,
  lote : string,
  vl_custo_medio : number,
  venda : number,
  qtde : number,
  motivo : string,
  mes_ano : string,
  mes_ano_validade : string
}

